import { UserIdsListToArrayPipe } from './user-ids-list-to-array.pipe';

describe('UserIdsListToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new UserIdsListToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
