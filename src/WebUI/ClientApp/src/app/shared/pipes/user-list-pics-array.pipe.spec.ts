import { UserListPicsArrayPipe } from './user-list-pics-array.pipe';

describe('UserListPicsArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new UserListPicsArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
