import { ToThemeClassColorPipe } from './to-theme-class-color.pipe';

describe('ToThemeClassColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ToThemeClassColorPipe();
    expect(pipe).toBeTruthy();
  });
});
